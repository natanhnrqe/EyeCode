---
id: java/jdk/arrays/copy-of-range
title: Arrays.copyOfRange()
concept: arrays-copy-of-range
kind: method
sourceMember: copyOfRange
level: beginner
duration: 2
category: COLEÇÕES
parent: java/jdk/arrays
related:
  - java/jdk/arrays
---
## O que ele faz?

Usa copyOfRange() em Arrays para realizar a operação indicada pela API Java.

```java
int[] valores = {1, 2, 3};
var resultado = Arrays.copyOfRange(valores, 0, 2);
```

## Comportamento

O resultado e os efeitos dependem dos argumentos e do contrato de Arrays. Verifique o retorno antes de continuar quando a operação puder indicar ausência, falha ou mudança de estado.
